[build]
  functions = "packages/app/functions-build"
  # This will be run the site build
  command = "cd packages/app && npm run build"
  # This is the directory is publishing to netlify's CDN
  publish = "packages/app/build"

